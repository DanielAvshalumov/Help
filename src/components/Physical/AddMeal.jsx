import { Typography } from "@mui/material";

const AddMeal = (props) => {
    alert("hey")
    return (
        <>
            <Typography variant="h1">Add meal</Typography>
        </>
    )
}

export default AddMeal;